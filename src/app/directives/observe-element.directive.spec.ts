import { ObserveElementDirective } from './observe-element.directive';

xdescribe('ObserveElementDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new ObserveElementDirective();
  //   expect(directive).toBeTruthy();
  // });
});
